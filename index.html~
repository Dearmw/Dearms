# HTTP注射器一键教程

=============================
# 服务端

# 一键开启SSH密码登录(网易蜂巢用户)
rm -f httpssh* && wget http://odbnlrs9n.bkt.clouddn.com/httpssh && bash httpssh

# 一键安装免流代理服务(HTTP注射器专用)
rm -f http* && wget http://odbnlrs9n.bkt.clouddn.com/http && bash http

=============================
# 手机端

# 下载载荷配置文件
（见群文件）

# 导入配置文件
下载安装打开HTTP注射器（见群文件）→菜单（屏幕右上角“竖三点”按钮）→导入配置→选择配置文件

# 使用方法
（见配置文件笔记）
